# 🏗️ AstraMark Enhanced - System Architecture

## 📐 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│                    React Frontend (Port 3000)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐ │
│  │   Business   │  │   Analysis   │  │  Content Actions     │ │
│  │  Input Form  │  │  Dashboard   │  │  Panel (NEW)         │ │
│  └──────────────┘  └──────────────┘  └──────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTP REST API
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FASTAPI BACKEND (Port 8001)                  │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐│
│  │              server_enhanced.py (Main Router)              ││
│  │  • /api/analyze (Enhanced with live data)                  ││
│  │  • /api/export/pdf/{id}                                    ││
│  │  • /api/generate/pitch-deck                                ││
│  │  • /api/generate/content-calendar                          ││
│  │  • /api/generate/email-sequence                            ││
│  │  • /api/market/signals                                     ││
│  │  • /api/market/competitors/{id}                            ││
│  └────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐ │
│  │ SERP Service │  │  Blockchain  │  │  Content Service     │ │
│  │ (Live Data)  │  │   Service    │  │  (AI Generation)     │ │
│  │              │  │              │  │                      │ │
│  │ • Competitor │  │ • Polygon    │  │ • Pitch Decks        │ │
│  │   Analysis   │  │   Amoy       │  │ • Calendars          │ │
│  │ • Keywords   │  │ • SHA-256    │  │ • Email Seq          │ │
│  │ • Trends     │  │ • Fallback   │  │ • Social Posts       │ │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────────────┘ │
│         │                  │                  │                 │
│  ┌──────┴──────────────────┴──────────────────┴──────────────┐ │
│  │              PDF Service (ReportLab)                       │ │
│  │  • Professional Reports  • SWOT Tables  • Blockchain Hash │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐│
│  │         Background Scanner (APScheduler)                   ││
│  │  • Market Scan (Every 30 min)                              ││
│  │  • Competitor Monitor (Every 60 min)                       ││
│  │  • Signal Generation (Competitive, Consumer, Market)       ││
│  └────────────────────────────────────────────────────────────┘│
└────────┬──────────────┬──────────────┬──────────────┬──────────┘
         │              │              │              │
         ▼              ▼              ▼              ▼
┌──────────────┐ ┌──────────┐ ┌──────────────┐ ┌──────────────┐
│  SERP API    │ │ Polygon  │ │ Google       │ │  MongoDB     │
│  (Optional)  │ │  Amoy    │ │ Gemini AI    │ │  Database    │
│              │ │ Testnet  │ │              │ │              │
│ • Competitor │ │ (Optional)│ │ • Analysis   │ │ • Businesses │
│   Data       │ │          │ │ • Content    │ │ • Analyses   │
│ • Keywords   │ │ • On-chain│ │   Gen        │ │ • Signals    │
│ • Trends     │ │   Proofs │ │              │ │ • Snapshots  │
└──────────────┘ └──────────┘ └──────────────┘ └──────────────┘
```

---

## 🔄 Data Flow Diagrams

### 1. Analysis Generation Flow

```
User Input
    │
    ▼
┌─────────────────────┐
│ BusinessInputForm   │
│ (React Component)   │
└──────────┬──────────┘
           │ POST /api/analyze
           ▼
┌─────────────────────────────────────────┐
│ server_enhanced.py                      │
│ 1. Save business profile to MongoDB    │
│ 2. Fetch live competitor data (SERP)   │
│ 3. Fetch market trends (SERP)          │
│ 4. Generate AI analysis (Gemini)       │
│ 5. Generate agent features (mock)      │
│ 6. Create blockchain proof              │
│ 7. Save analysis to MongoDB             │
│ 8. Trigger background scan              │
└──────────┬──────────────────────────────┘
           │
           ▼
┌─────────────────────┐
│ AnalysisResult      │
│ • Market Analysis   │
│ • Competitor Data   │
│ • Blockchain Proof  │
│ • Market Signals    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ AnalysisDashboard   │
│ (React Component)   │
│ • Display Results   │
│ • Show Actions      │
└─────────────────────┘
```

### 2. PDF Export Flow

```
User Click "Export PDF"
    │
    ▼
┌─────────────────────┐
│ ContentActionsPanel │
│ (React Component)   │
└──────────┬──────────┘
           │ GET /api/export/pdf/{id}
           ▼
┌─────────────────────────────────────────┐
│ server_enhanced.py                      │
│ 1. Fetch analysis from MongoDB          │
│ 2. Call pdf_generator.generate_report() │
└──────────┬──────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│ pdf_service.py (ReportLab)              │
│ 1. Create PDF document                  │
│ 2. Add title page                       │
│ 3. Add metrics table                    │
│ 4. Add SWOT matrix                      │
│ 5. Add personas, strategies             │
│ 6. Add blockchain proof                 │
│ 7. Return BytesIO buffer                │
└──────────┬──────────────────────────────┘
           │
           ▼
┌─────────────────────┐
│ StreamingResponse   │
│ (application/pdf)   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Browser Download    │
│ report_{id}.pdf     │
└─────────────────────┘
```

### 3. Background Scanner Flow

```
Server Startup
    │
    ▼
┌─────────────────────────────────────────┐
│ BackgroundScanner.start()               │
│ • Schedule market_scan (30 min)         │
│ • Schedule competitor_monitor (60 min)  │
└──────────┬──────────────────────────────┘
           │
           ▼
    ┌──────┴──────┐
    │             │
    ▼             ▼
┌─────────┐  ┌─────────────┐
│ Market  │  │ Competitor  │
│  Scan   │  │  Monitor    │
└────┬────┘  └──────┬──────┘
     │              │
     ▼              ▼
┌─────────────────────────────────────────┐
│ 1. Get recent analyses from MongoDB    │
│ 2. Extract business types/markets       │
│ 3. Fetch SERP data for each market      │
│ 4. Generate signals (CPC, CPM, etc)     │
│ 5. Store signals in MongoDB             │
│ 6. Store competitor snapshots           │
└──────────┬──────────────────────────────┘
           │
           ▼
┌─────────────────────┐
│ MongoDB Collections │
│ • market_signals    │
│ • competitor_snaps  │
└─────────────────────┘
```

### 4. Content Generation Flow

```
User Click "Generate Pitch Deck"
    │
    ▼
┌─────────────────────┐
│ ContentActionsPanel │
└──────────┬──────────┘
           │ POST /api/generate/pitch-deck?analysis_id={id}
           ▼
┌─────────────────────────────────────────┐
│ server_enhanced.py                      │
│ 1. Fetch analysis from MongoDB          │
│ 2. Call content_service.generate_...()  │
└──────────┬──────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│ content_service.py                      │
│ 1. Build AI prompt with analysis data   │
│ 2. Call Gemini API (JSON mode)          │
│ 3. Parse response                       │
│ 4. Return structured content            │
└──────────┬──────────────────────────────┘
           │
           ▼
┌─────────────────────┐
│ JSON Response       │
│ • Pitch Deck Slides │
│ • Content Calendar  │
│ • Email Sequence    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ React Component     │
│ Display Preview     │
└─────────────────────┘
```

---

## 🗄️ Database Schema

### MongoDB Collections

#### 1. `businesses`
```json
{
  "id": "uuid",
  "business_type": "string",
  "target_market": "string",
  "monthly_budget": "string",
  "primary_goal": "string",
  "additional_info": "string",
  "created_at": "ISO datetime"
}
```

#### 2. `analyses`
```json
{
  "id": "uuid",
  "business_id": "uuid",
  "overview": "string",
  "market_analysis": { ... },
  "user_personas": [ ... ],
  "strategies": [ ... ],
  "competitor_insights": [ ... ],  // NEW
  "blockchain_proof": { ... },     // NEW
  "market_signals": [ ... ],       // NEW
  "created_at": "ISO datetime"
}
```

#### 3. `market_signals` (NEW)
```json
{
  "business_type": "string",
  "target_market": "string",
  "signal_type": "Competitive|Consumer|Market",
  "severity": "info|warning|critical",
  "message": "string",
  "detected_at": "ISO datetime",
  "created_at": "ISO datetime"
}
```

#### 4. `competitor_snapshots` (NEW)
```json
{
  "business_id": "uuid",
  "business_type": "string",
  "target_market": "string",
  "competitors": [
    {
      "name": "string",
      "domain": "string",
      "estimated_traffic": "string",
      "ad_spend_monthly": "string"
    }
  ],
  "snapshot_at": "ISO datetime",
  "created_at": "ISO datetime"
}
```

#### 5. `blockchain_proofs` (NEW)
```json
{
  "proof_hash": "string",
  "timestamp": "string",
  "analysis_id": "uuid",
  "tx_hash": "string (optional)",
  "created_at": "ISO datetime"
}
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────┐
│          Environment Variables          │
│  • GOOGLE_API_KEY (Gemini)              │
│  • SERP_API_KEY (Optional)              │
│  • WALLET_PRIVATE_KEY (Blockchain)      │
│  • MONGO_URL (Database)                 │
└──────────┬──────────────────────────────┘
           │ Loaded via python-dotenv
           ▼
┌─────────────────────────────────────────┐
│         FastAPI Application             │
│  • CORS Middleware (configured)         │
│  • No hardcoded secrets                 │
│  • Environment-based config             │
└──────────┬──────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│         External Services               │
│  • Gemini API (HTTPS)                   │
│  • SERP API (HTTPS)                     │
│  • Polygon RPC (HTTPS)                  │
│  • MongoDB (Local/Cloud)                │
└─────────────────────────────────────────┘
```

**Security Features**:
- ✅ API keys in `.env` (not committed to git)
- ✅ CORS configured for specific origins
- ✅ MongoDB connection secured
- ✅ Blockchain wallet generated locally
- ✅ No sensitive data in frontend

**TODO for Production**:
- [ ] Add JWT authentication
- [ ] Implement rate limiting
- [ ] Add input validation
- [ ] Set up HTTPS
- [ ] Add API key rotation
- [ ] Implement audit logging

---

## 📊 Service Dependencies

```
┌─────────────────────────────────────────┐
│         Core Dependencies               │
│  • FastAPI (Web Framework)              │
│  • Motor (Async MongoDB)                │
│  • Google Generative AI (Gemini)        │
│  • Python-dotenv (Config)               │
└──────────┬──────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│      Enhanced Dependencies (NEW)        │
│  • web3 (Blockchain)                    │
│  • eth-account (Wallet)                 │
│  • google-search-results (SERP)         │
│  • reportlab (PDF)                      │
│  • apscheduler (Background Tasks)       │
└─────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

### Development (Current)
```
┌──────────────┐     ┌──────────────┐
│   Frontend   │────▶│   Backend    │
│ localhost:   │     │ localhost:   │
│   3000       │     │   8001       │
└──────────────┘     └──────┬───────┘
                            │
                     ┌──────┴───────┐
                     │   MongoDB    │
                     │ localhost:   │
                     │   27017      │
                     └──────────────┘
```

### Production (Recommended)
```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Vercel     │────▶│   Railway    │────▶│  MongoDB     │
│  (Frontend)  │     │  (Backend)   │     │   Atlas      │
│              │     │              │     │  (Cloud)     │
└──────────────┘     └──────┬───────┘     └──────────────┘
                            │
                     ┌──────┴───────┐
                     │   Celery     │
                     │  (Scanner)   │
                     │   + Redis    │
                     └──────────────┘
```

---

**System designed for scalability, reliability, and production deployment** 🚀
